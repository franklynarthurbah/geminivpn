// ============================================================
// UPDATED PRISMA SCHEMA - merge into prisma/schema.prisma
// Run: npx prisma migrate dev --name add_demo_system
// ============================================================

// --------------- ADD THESE MODELS ---------------

model DemoAccount {
  id               String   @id @default(cuid())
  userId           String   @unique
  user             User     @relation(fields: [userId], references: [id])
  username         String   @unique
  creatorIp        String
  expiresAt        DateTime
  maxClients       Int      @default(1)
  bandwidthMbps    Int      @default(10)
  allowedServers   String   @default("us-ny,eu-london")
  convertedToPaid  Boolean  @default(false)
  conversionDate   DateTime?
  warningSent      Boolean  @default(false)
  isDeleted        Boolean  @default(false)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([creatorIp])
  @@index([expiresAt])
  @@index([isDeleted])
}

// --------------- ADD THESE FIELDS TO User model ---------------
// demoAccount   DemoAccount?
// isActive      Boolean   @default(true)
// isTestUser    Boolean   @default(false)
// lastLoginAt   DateTime?
// subscriptionEndsAt  DateTime?
// gracePeriodEndsAt   DateTime?
// stripeCustomerId    String?   @unique
// stripeSubscriptionId String?  @unique

// --------------- ADD THESE FIELDS TO VPNClient model ---------------
// clientName    String?
// qrCodeData    String?   @db.Text
// configFile    String?   @db.Text
// isConnected   Boolean   @default(false)
// serverId      String?
// server        VPNServer? @relation(fields: [serverId], references: [id])

// --------------- ADD THESE FIELDS TO VPNServer model ---------------
// maxClients    Int       @default(200)
// dnsServers    String    @default("1.1.1.1,1.0.0.1")
// clients       VPNClient[]
// isActive      Boolean   @default(true)
// tags          String[]  @default([])

// --------------- ENUM additions ---------------
// Add TRIAL to SubscriptionStatus if not present:
// enum SubscriptionStatus {
//   INACTIVE
//   TRIAL    // <- add this
//   ACTIVE
//   EXPIRED
//   CANCELLED
// }
